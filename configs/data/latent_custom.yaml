_target_: src.data.latentDataModule.LatentDataModule

# root where you ran the caching script:
root_dir: /shared/qd8/vae_latent_nbin3

batch_size: 4
num_workers: 4
pin_memory: false
seed: 51

depth_transform_args:
  type: reversible_normalization
  clip: true
  norm_min: -1.0
  norm_max: 1.0
  min_max_quantile: 0.02
  max_depth: 500

